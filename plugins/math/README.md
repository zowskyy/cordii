# math — deterministic math, datetime, and units pool

Zero-token deterministic plugins for exact computation. These handle math, datetime, and unit conversions so the model does not have to.

## Plugins

- MathRouter, MathPipeline, SymbolicEngine, MathVerifier
- DateTimeRouter, DateTimeEngine
- UnitsRouter, UnitsEngine
