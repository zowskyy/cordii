# model — model adapters and embeddings

Model-facing plugins that wrap local/remote providers and optional embedding services.

## Plugins

- OllamaModel — Ollama provider
- EmbeddingModel — optional embedding provider (full profile only)
